{
    "devDependencies": {
        "@11ty/eleventy": "^3.1.2",
        "@11ty/eleventy-img": "^6.0.4",
        "bootstrap": "^5.3.8",
        "html-minifier-terser": "^7.2.0",
        "sass": "^1.97.1"
    },
    "scripts": {
        "watch:sass": "npx sass scss/:netherpi.net/css/ --style compressed -w --update --no-source-map",
        "watch:eleventy": "npx @11ty/eleventy --serve --watch --incremental",
        "watch": "npm run watch:sass & npm run watch:eleventy"
    }
}
